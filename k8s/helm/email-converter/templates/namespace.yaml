apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "email-converter.namespace" . }}
  labels:
    {{- include "email-converter.labels" . | nindent 4 }}
    app.kubernetes.io/component: infrastructure
  annotations:
    description: "Email Converter SaaS namespace"
    managed-by: helm
