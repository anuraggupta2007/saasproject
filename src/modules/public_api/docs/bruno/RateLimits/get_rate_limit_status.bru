meta {
  name: Get Rate Limit Status
  type: http
  seq: 9
}

get {
  url: {{base_url}}/api/public/v1/rate-limit/status
  auth: none
}

script {
  req.header("Authorization", "Bearer {{api_key}}")
}

assert {
  res.status: eq 200
}
