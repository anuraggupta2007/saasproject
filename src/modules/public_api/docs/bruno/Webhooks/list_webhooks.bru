meta {
  name: List Webhooks
  type: http
  seq: 8
}

get {
  url: {{base_url}}/api/public/v1/webhooks
  auth: none
}

script {
  req.header("Authorization", "Bearer {{api_key}}")
}

assert {
  res.status: eq 200
}
