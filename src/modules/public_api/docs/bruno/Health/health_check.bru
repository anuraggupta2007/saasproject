meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/public/v1/health
  auth: none
}

script {
  req.header("Authorization", "Bearer {{api_key}}")
  req.header("X-API-Version", "1")
}

assert {
  res.status: eq 200
  res.body.status: eq "healthy"
}
